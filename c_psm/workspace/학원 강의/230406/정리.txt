클래스 구조체 함수

구조체
사용자가 정한 자료형
구조체는 변수를 묶어놓는 곳
클래스랑 다름

함수
동작을 모아놓는 곳

ex)뛰기, 춤추기 

구조체(공용체) 변수만 모아두는 곳
ex) 이름, 주소, 나이,전화번호 
공용체 = 선언된 변수들이 동일한 메모리를 사용한다.
구조체 = 선언된 변수들이 각각의 메모리를 부여한다.

클래스 함수와 구조체를 모아둔것

클래스 = 구조체 + 함수
함수: 동작들의 모임
구조체는: 데이터의 모임 




main함수 3가지
1. 구조체(공용체)
2. 함수
3. 클래스

///////////////////////////////////////////////////////
C프로그램의 구성
C언어는 한 개 이상의 함수로 프로그램이 작성이 되고 프로그램 
실행 시 가장 먼저 호출 되는 함수는 main함수이다.

반드시!!! 한 개의 main함수가 존재해야한다.

프로그램의 기본구조
C언어는 대문자와 소문자를 구별하는데 일반적으로 소문자를 기초하여 작성한다.
모든 명령뮨은 반드시 세미콜론으로 끝나야 한다.
단 #으로 시작하는 구문 뒤에는  세미콜론을 붙이지 않는다.

프로그램 기본 구조

1) int main()
- 자료형인 main함수를 불러 오겠다
-변수가 없다.
-C프로그램은 main함수에서 시작된다. main다음의 괄호()는 컴파일러에게 main이 함수라는 것을 알려준다

2) { }
-중괄호는 여러 문장들을 함께 묶는데 사용된다. 
- main함수 다음의 중괄호는 함수의 시작과 끝을 의미하게 된다

3) return 0;
-프로그램이 정상 종료를 의미한다

\n: 줄바꿈
\r: 줄의 처음으로 이동
\b: 왼쪽으로 커서이동
\\: 백슬러시 문자 출력
\t: 탭 간격만큼 이동 (8byte)
\a: 경고음 출력
\0: 널문자
\" :큰 다옴표 출력

데이터형 / 변수/ 상수
데이터형 - 정수,실수,문자를 구분하며 도한 데이터의 범위나 부호의 사용여부 등을 결정짓는 것을 말한다.

정수 데이터형
int 
정수로 출력
자연수나 음수
소수점 이하를 표현하지 못함
무한대의 범위는 지원하지 않음
unsigned :음수를  표현할 수 없다.

실수 데이터형
소수점 이하를 표현함 
unsigned쓸 수 없다. 

고정소수점 방식:실수의 정확한 값을 저장할 수 있고 저장 방법이 단순하기는 하지만 전혀 치밀하지 못하고 효율적이지도 않다.
실수를 부호, 정수부와 소수부로 나눈다

부동소수점 방식: 부동소수점으로 표현되는 실수는 여러 개 존재할 수 있기 때문에 가수부의 정수부는 0이 아닌 한자리로 제한한다. 부호,지수부,가수부 나뉜다

문자형 데이더
char형 
-컴퓨터는 0과 1만 가지고 데이토를 저장하기 때문에 문자를 표현할 수 없다. 아스키코드를 사용하여 문자를 표현한다

변수
- 변경이 가능하다
-데이터를 저장하기 위한 공간으로 저장된 값의 변경이 가능하다
변수 선언
-자료형 변수명;
-변수 선언을 하면 메모리 공간을 할당 받아 사용할 수 있게 된다.
-최초 변수의 선언시 의미 없는 값이 저장되는데 이것을 쓰리기 값이라고 한다


변수 작성 규칙
1. 스페이스바 사용 안됨(공백 포함 안됨)
2.숫자로 시작하면 안된다
3.예약어 사용불가능
4.특수문자 안됨_
5.대소문자 구분한다

변수 초기화
-변수에 값을 대입하는 과정

출력 변환 문자
%d : 10진 정수 출력
%f: 고정 소수점 출력(소수점 이하 6자리 출력)
%c:문자 출력
%s: 문자열 출력

상수- 변경할 수 없는 값

스트림 - 운영체제는 키보드,모니터,프린터 스트림이라는 동등한 장치로 다룬다. 
입.출력 장치는 다르지만 스트림이라는 논리적인 동등한 장치로 표현되기 때문에
동일한 방법으로 입출력이 가능하다

입력함수 
scanf("입력 변환문자",&변수);
%ld
%lf
getchar
한 글자 가져오는 함수
ex)변수 = getchar();

getch
-문자열 

출력함수
printf("출력 변환문자",변수);
%e: 지수 출력
%d :정수 출력
%c: 문자 출력~
%o: 8진수 출력~
%p
%%
%u

산술 연산자
+-*/%
%연산자는 정수 연산에서만 사용된다.
정 정 = 정
정 실 = 실
소수점 아래 버리고 나오고

음수 소슈점 아래를 올려서 나와요

증감 연산자
++ --
변수에 저장된 값을 1증가 시키거나 감소 시키는 거
++변수 : 증가 하고 출력
변수++: 연산 수행 후 변수에 저장된 값을 증감














