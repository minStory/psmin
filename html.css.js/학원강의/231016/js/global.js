(()=>{
    console.log(name2);
    console.log(window);
})();

// 자바스크립트 환경
// 브라우저 환경 => 전역 객체는 window
// 노드.js 환경 => 전역 객체는 global

// JavaScript => window와 ECMAScript가 합쳐진 것

// ECMAScript => 환경과 상관없이 공통적으로 사용할 수 있는 것

// ES6(2015) => 변수 추가(let, const)